import 'package:flutter/material.dart';

class SplahsViewBody extends StatelessWidget {
  const SplahsViewBody({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column(
      children: [],
    );
  }
}
